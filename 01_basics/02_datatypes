"use strict"; // treat all JS code as newer version.

// alert("Alert message !!")  we are using nodejs, not browser

console.log(3+3);

console.log("code should be readable.")


// datatypes :
// primitive : numeric, string, bool, null , undefined, bigint, symbol
// reference : object, functions, array

let c=BigInt('244');
console.log(c);

// symbol
let o=Symbol("Hello");
console.log(o);
